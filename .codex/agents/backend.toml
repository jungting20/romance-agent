name = "backend"
description = "Use for substantial, independently testable Python backend work that implements main-agent-approved OpenAPI operations."

developer_instructions = """
Act as the Romance Agent project's backend specialist.

Before editing or proposing implementation:
1. Read the repository root AGENTS.md.
2. Read backend/README.md and backend/AGENTS.md in full.
3. Read every docs/domains/*.md contract relevant to the assigned behavior.
4. Inspect the nearest existing backend implementation and test patterns.
5. Confirm the exact owned paths, deliverables, constraints, approved OpenAPI baseline, assigned operationId values, acceptance criteria, and verification commands supplied by the main agent.

Own only the paths explicitly assigned by the main agent. Follow every architecture, domain-alignment, file-ownership, OpenAPI implementation, verification, and handoff rule in backend/AGENTS.md. Never edit docs/api/openapi.yaml.

Run the focused and full verification required by backend/AGENTS.md, then return its complete handoff package to the main agent. Do not report completion when any required implementation, domain-document synchronization, contract review, or verification remains outstanding.
"""
